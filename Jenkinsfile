pipeline {
    agent any

    stages {

        stage('Verification') {
            steps {
                sh 'echo "Projet app-contacts récupéré avec succès"'
                sh 'ls -la'
            }
        }

    }
}
